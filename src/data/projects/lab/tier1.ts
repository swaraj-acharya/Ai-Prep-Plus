import { defineProject } from "./types";

export const TIER1 = [
  defineProject({
    id: "lab-typed-llm-service", name: "Typed LLM Extraction Service", tier: 1, categories: ["AI", "Backend"],
    hours: [14, 20], unlockWeek: 15,
    roadmap: { weeks: [4, 14, 15], mastery: ["m-python", "m-llm"], resources: ["fastapi", "pydantic", "anthropic-docs", "openai-docs"], dsa: ["Arrays & Hashing"], gates: ["P2"] },
    prerequisites: [],
    objective: "Turn unstructured text into validated, typed records through an HTTP API — and measure how often the model is actually right.",
    problem: "Most LLM integrations return free text that downstream code parses with hope. Teams need schema-guaranteed outputs, typed errors and an accuracy number before they trust extraction in production.",
    users: ["Backend teams adding extraction to existing services", "Ops teams processing emails, tickets or forms"],
    skills: ["Structured outputs / tool calling", "Pydantic validation", "Retry-on-validation-error loops", "Labelled evaluation sets"],
    tech: ["Python 3.12", "FastAPI", "Pydantic v2", "Claude / OpenAI SDK", "pytest", "Docker"],
    arch: {
      api: "FastAPI: POST /extract/{schema} with request IDs, SSE streaming for long docs",
      app: "Schema registry (Pydantic models) → prompt builder → provider call → validate → repair-retry (max 2)",
      data: "SQLite/Postgres table of requests, raw output, validation errors, latency, tokens",
      ai: "Native structured output / tool calling; temperature 0; per-schema few-shot examples",
      observability: "Structured JSON logs with request_id, model, tokens, latency, retries",
    },
    features: [
      "Three production-shaped schemas (invoice, support ticket, job posting) with nested fields and enums",
      "Validation-error feedback loop: the exact Pydantic error is sent back once for self-repair",
      "Per-field confidence and a `needs_review` flag when required fields are empty or low-confidence",
      "Typed error taxonomy (provider_timeout, schema_violation, content_refusal) returned as RFC 7807 problem+json",
      "Offline eval command that scores field-level accuracy against 50 hand-labelled samples",
    ],
    milestones: ["Schema registry + one schema end-to-end", "Repair loop + typed errors", "50-sample labelled set + eval script", "Dockerized with README results table"],
    testing: ["Unit tests for every schema with valid/invalid fixtures", "Recorded provider responses (VCR-style) so CI never calls a paid API", "Eval run fails if field accuracy drops below the committed baseline"],
    observability: ["Log tokens in/out and latency per request", "Count repairs per schema"],
    security: ["API key via env/secret store only", "Input size limits and content-type checks"],
    deployment: ["Docker image under 200 MB", "One-command run with docker compose"],
    benchmarks: ["Field-level accuracy per schema (target ≥ 92%)", "Repair-loop rescue rate", "p50/p95 latency and cost per 1k documents"],
    stretch: ["Compare two models on the same eval and publish the cost/accuracy trade-off", "Add a JSON-schema import so non-Python users can register schemas"],
  }),
  defineProject({
    id: "lab-resilient-llm-client", name: "Resilient LLM Client Library", tier: 1, categories: ["AI", "DevTools"],
    hours: [16, 24], unlockWeek: 15,
    roadmap: { weeks: [4, 13, 15, 25], mastery: ["m-llm", "m-js"], resources: ["anthropic-docs", "openai-docs", "litellm", "vitest"], dsa: ["Linked List", "Heap / Priority Queue"], gates: ["P2"] },
    prerequisites: [],
    objective: "Publish a small, well-tested TypeScript SDK that makes any LLM call production-safe: timeouts, retries with jitter, fallbacks, streaming, caching and token accounting.",
    problem: "Every app re-implements retry loops, provider fallbacks and cost tracking badly. A focused library with honest tests is a reusable building block and a credible open-source artifact.",
    users: ["Node/Next.js developers shipping AI features", "Your own later Lab projects (gateway, SaaS kit)"],
    skills: ["Exponential backoff with full jitter", "Circuit breakers", "Streaming (SSE/async iterators)", "LRU caching", "Semantic versioning"],
    tech: ["TypeScript", "Node 22", "Vitest", "tsup", "npm publishing", "GitHub Actions"],
    arch: {
      app: "Middleware pipeline: timeout → retry → circuit breaker → cache → provider adapter; each a composable function",
      data: "Pluggable cache interface (in-memory LRU, Redis adapter)",
      ai: "Provider adapters (Anthropic, OpenAI-compatible) behind one typed interface; fallback chain by error class",
      observability: "Hook API emitting request/response/retry events with token + cost fields",
    },
    features: [
      "Retry only on retryable errors (429/5xx/timeouts), honour Retry-After, never retry non-idempotent tool side effects",
      "Circuit breaker per provider with half-open probing",
      "Fallback chain (e.g. primary model → cheaper model → cached answer) configured declaratively",
      "Streaming as an async iterator with mid-stream failure handling",
      "Token + cost accounting per call with a pricing table you can update",
    ],
    milestones: ["Adapter interface + one provider", "Retry/timeout/circuit breaker with fake-clock tests", "Streaming + fallbacks", "Published v0.1.0 with docs site and CHANGELOG"],
    testing: ["Deterministic tests with fake timers and a fake provider that injects 429/500/timeouts", "Property test: retries never exceed the configured budget", "Type tests for the public API"],
    observability: ["Event hooks documented with an OpenTelemetry example"],
    security: ["Never log prompt bodies by default; redact API keys in errors"],
    deployment: ["Published to npm with provenance from GitHub Actions"],
    benchmarks: ["Success rate under 20% injected failure (target ≥ 99.5%)", "Added latency overhead per call (target < 2 ms)", "Cache hit rate on a replayed workload"],
    stretch: ["Python port with identical semantics", "Hedged requests for tail latency"],
  }),
  defineProject({
    id: "lab-embedding-lab", name: "Embedding & Chunking Benchmark Lab", tier: 1, categories: ["RAG", "Evaluation", "AI"],
    hours: [14, 22], unlockWeek: 20,
    roadmap: { weeks: [5, 19, 20], mastery: ["m-rag", "m-data"], resources: ["pgvector", "hf-llm", "ragas", "pinecone-learn"], dsa: ["Heap / Priority Queue", "Math & Geometry"], gates: ["P3"] },
    prerequisites: [],
    extends: ["pr-atlas"],
    objective: "Stop guessing chunk sizes and embedding models: build a reproducible harness that measures retrieval quality on YOUR corpus.",
    problem: "RAG quality is mostly decided before generation — by chunking and embeddings — yet most teams pick defaults from a blog post. A benchmark lab turns those choices into data.",
    users: ["Engineers starting a RAG system", "You, before building AtlasAI's retrieval layer"],
    skills: ["Cosine similarity and vector norms", "Chunking strategies (fixed, recursive, semantic, structure-aware)", "Recall@k, MRR, nDCG", "Experiment design"],
    tech: ["Python", "sentence-transformers", "pgvector", "pandas", "matplotlib", "Typer CLI"],
    arch: {
      app: "CLI: ingest → chunk (strategy plug-ins) → embed (model plug-ins) → index → evaluate",
      data: "Postgres + pgvector; results in a tidy CSV per run",
      ai: "3+ embedding models (local + API), synthetic question generation for the golden set with manual review",
      observability: "Run manifest (git SHA, params, dataset hash) saved with every result",
    },
    features: [
      "Four chunkers including a markdown/heading-aware splitter and a table-preserving splitter",
      "Golden set of 100 question → relevant-chunk pairs (LLM-drafted, human-verified)",
      "Metrics: recall@1/5/10, MRR, nDCG@10, index size, embedding cost and time",
      "HTML report comparing every (chunker × model) cell with a recommendation",
    ],
    milestones: ["Corpus + golden set", "Chunkers + one model end-to-end", "Full grid run", "Published report with a decision"],
    testing: ["Unit tests for chunk boundaries (no split inside code blocks/tables)", "Metric functions tested against hand-computed examples"],
    deployment: ["Reproducible with `make bench` from a clean clone"],
    benchmarks: ["Recall@5 and MRR per configuration", "Embedding throughput (chunks/sec)", "Cost per 10k chunks"],
    stretch: ["Add Matryoshka/dimension-truncation experiments", "Quantized vectors (binary/int8) vs float recall"],
  }),
  defineProject({
    id: "lab-idempotent-job-runner", name: "Idempotent Job Runner", tier: 1, categories: ["Backend", "Distributed Systems"],
    hours: [16, 24], unlockWeek: 13,
    roadmap: { weeks: [11, 12, 13], mastery: ["m-os", "m-db"], resources: ["bullmq", "redis", "postgres", "docker"], dsa: ["Heap / Priority Queue", "Linked List"], gates: ["P2"] },
    prerequisites: [],
    extends: ["pr-paircraft"],
    objective: "Build the job-processing core every AI system needs: at-least-once delivery made safe with idempotency, retries, backoff and a dead-letter queue.",
    problem: "AI workloads are slow, flaky and expensive; naive queues double-charge, lose jobs on deploys and hide poison messages. Correct job semantics are a core backend skill interviewers probe.",
    users: ["Backend teams running AI or media jobs", "PairCraft's code-execution queue (W12)"],
    skills: ["At-least-once vs exactly-once semantics", "Idempotency keys", "Visibility timeouts / leases", "Graceful shutdown", "Postgres SKIP LOCKED"],
    tech: ["TypeScript", "Node", "BullMQ + Redis", "PostgreSQL", "Docker Compose", "Vitest"],
    arch: {
      api: "POST /jobs with Idempotency-Key header; GET /jobs/:id",
      app: "Producer dedupes by key; workers with concurrency limits and heartbeat leases",
      data: "Postgres jobs table (state machine) + Redis queue; results table keyed by idempotency key",
      workers: "Retries with exponential backoff + jitter, max attempts, DLQ with replay tool",
      observability: "Prometheus metrics: queue depth, age of oldest job, success/failure/retry counts",
    },
    features: [
      "Explicit job state machine: queued → running → succeeded | failed | dead",
      "Idempotent side effects: result recorded before ack; replays return the stored result",
      "Graceful shutdown drains in-flight jobs; crash recovery via expired leases",
      "DLQ inspector CLI with replay and purge",
      "Chaos script that kills workers mid-job and proves no duplicate side effects",
    ],
    milestones: ["State machine + Postgres schema", "Workers with retries/backoff", "Idempotency + DLQ", "Chaos test + metrics dashboard"],
    testing: ["Integration tests against real Redis/Postgres in Docker", "Chaos test asserts exactly-once effects under worker kills", "Unit tests for backoff schedule"],
    observability: ["Grafana dashboard for depth, latency, failures"],
    security: ["Job payload size limits; no secrets in payloads"],
    deployment: ["docker compose with Redis, Postgres, 3 workers"],
    benchmarks: ["Throughput (jobs/sec) at 1/4/16 workers", "Duplicate side effects under chaos (target 0)", "p95 enqueue→start latency"],
    stretch: ["Swap BullMQ for Postgres-only (SKIP LOCKED) and compare", "Priority lanes and per-tenant fairness"],
  }),
  defineProject({
    id: "lab-nextjs-ai-kit", name: "Next.js AI Feature Kit", tier: 1, categories: ["AI + MERN", "MERN", "SaaS"],
    hours: [18, 26], unlockWeek: 16,
    roadmap: { weeks: [8, 15, 16], mastery: ["m-mern", "m-js", "m-llm"], resources: ["nextjs", "react-dev", "node-docs", "anthropic-docs", "playwright"], dsa: ["Sliding Window"], gates: ["P2"] },
    prerequisites: [],
    objective: "Build the reusable MERN+AI foundation you will fork for later products: auth, streaming UI, per-user token metering, rate limits and a clean server/client split.",
    problem: "Every AI product needs the same unglamorous plumbing. Doing it once, correctly, with tests turns your MERN experience into an AI-product accelerator.",
    users: ["You (base for Lab SaaS projects)", "Indie developers shipping an AI feature fast"],
    skills: ["Next.js App Router + Server Actions", "Streaming responses (ReadableStream/SSE)", "Token-bucket rate limiting", "Usage metering"],
    tech: ["Next.js", "TypeScript", "PostgreSQL + Drizzle", "Auth (OAuth)", "Tailwind", "Playwright"],
    arch: {
      frontend: "Streaming UI with optimistic updates, abort/cancel, error recovery",
      api: "Route handlers with zod validation; streaming endpoint with backpressure",
      app: "Usage service: pre-flight budget check → call → post-hoc token accounting",
      data: "Postgres: users, usage_events, quotas; Redis (optional) for rate-limit buckets",
      ai: "Provider-agnostic call through Lab 2's resilient client",
      observability: "Per-request log line with user, tokens, latency, cost",
    },
    features: [
      "One real AI feature (e.g. structured document summarizer) — not a chat clone",
      "Per-user daily token quota with graceful degradation messaging",
      "Sliding-window rate limiter on AI endpoints",
      "Streaming output with cancel and resume-on-reconnect",
      "Admin page: usage by user/day and cost",
    ],
    milestones: ["Auth + schema", "Streaming feature end-to-end", "Quota + rate limits", "E2E tests + deploy"],
    testing: ["Playwright E2E for sign-in → feature → quota exceeded", "Unit tests for the rate limiter", "Mocked provider for CI"],
    security: ["Server-only API keys", "CSRF-safe mutations", "Input size limits"],
    deployment: ["Vercel + managed Postgres with preview deployments"],
    benchmarks: ["Time-to-first-token p95", "Quota enforcement accuracy under concurrent requests"],
    stretch: ["Organizations/teams (becomes Lab 22)", "Stripe-ready usage export"],
  }),
  defineProject({
    id: "lab-llm-call-tracer", name: "LLM Call Tracer", tier: 1, categories: ["Observability", "AI"],
    hours: [12, 18], unlockWeek: 21,
    roadmap: { weeks: [21, 23], mastery: ["m-rag", "m-devops"], resources: ["otel", "langfuse", "prometheus", "grafana"], dsa: ["Trees"], gates: ["P3"] },
    prerequisites: [],
    objective: "Instrument LLM calls with OpenTelemetry so every request becomes a trace with tokens, cost, latency and prompt version attached.",
    problem: "Without traces, AI systems fail silently: a prompt change doubles cost or a retrieval step times out and nobody can see where. GenAI semantic conventions make this standard.",
    users: ["Teams running LLM features in production", "Your later observability projects"],
    skills: ["OpenTelemetry spans/attributes", "GenAI semantic conventions", "Trace context propagation", "Cost attribution"],
    tech: ["Python or TypeScript", "OpenTelemetry SDK", "OTel Collector", "Jaeger/Tempo", "Grafana"],
    arch: {
      app: "Decorator/wrapper that opens a span per LLM call with model, tokens, cost, prompt_version, cache_hit",
      observability: "OTel Collector → Tempo/Jaeger for traces, Prometheus for derived metrics, Grafana dashboards",
      infra: "docker compose stack for the full telemetry pipeline",
    },
    features: [
      "Nested spans for RAG pipelines (retrieve → rerank → generate) with timing breakdown",
      "Cost computed from a pricing table and attached as a span attribute",
      "Prompt/template version and hash on every span",
      "Grafana dashboard: p95 latency by model, cost per endpoint, error rate",
    ],
    milestones: ["Span wrapper + attributes", "Collector + backend running", "RAG pipeline instrumented", "Dashboard + README screenshots"],
    testing: ["In-memory span exporter asserts attributes and parent/child relations"],
    observability: ["Sampling configuration documented (head vs tail)"],
    security: ["Prompt/response capture is opt-in and redacted"],
    deployment: ["One docker compose file for app + collector + Grafana"],
    benchmarks: ["Instrumentation overhead per call", "Trace completeness (% of requests with full span tree)"],
    stretch: ["Tail-based sampling that keeps all errors and slow traces", "Export to Langfuse for side-by-side comparison"],
  }),
  defineProject({
    id: "lab-tool-agent", name: "Framework-free Tool-Calling Agent", tier: 1, categories: ["Agents", "AI"],
    hours: [16, 24], unlockWeek: 37,
    roadmap: { weeks: [36, 37], mastery: ["m-agents"], resources: ["anthropic-agents", "anthropic-docs", "agents-sdk"], dsa: ["Graphs", "Backtracking"], gates: ["P5"] },
    prerequisites: ["lab-typed-llm-service"],
    extends: ["pr-paper"],
    objective: "Write the agent loop yourself — tool registry, JSON-schema validation, step budgets, stop conditions and traces — then measure its success rate.",
    problem: "Frameworks hide the failure modes that matter in interviews and production: compounding step errors, runaway loops, invalid tool arguments. Building it bare teaches bounded autonomy.",
    users: ["Engineers evaluating agent frameworks", "You, before Paper-to-Prod"],
    skills: ["ReAct loop", "Tool schemas and argument validation", "Budgets (steps, tokens, wall-clock)", "Error compounding math"],
    tech: ["Python", "Pydantic", "Claude/OpenAI tool calling", "pytest"],
    arch: {
      app: "Loop: plan → tool call → validate args → execute with timeout → observe → stop check",
      ai: "Native tool calling; structured final answer schema; reflection step optional",
      observability: "Trajectory log (JSONL) per run for later evaluation",
    },
    features: [
      "Tool registry with JSON-schema generated from type hints",
      "Hard budgets: max steps, max tokens, max seconds, max spend",
      "Structured tool errors fed back to the model (not stack traces)",
      "Eval set of 30 multi-step tasks with automatic success checks",
    ],
    milestones: ["Loop + two tools", "Budgets + structured errors", "30-task eval", "Write-up: where it fails and why"],
    testing: ["Scripted fake model to test loop control deterministically", "Eval suite runnable in CI with recorded responses"],
    security: ["Tools run with least privilege; no shell tool without a sandbox"],
    deployment: ["CLI + optional FastAPI endpoint"],
    benchmarks: ["Task success rate", "Mean steps and tokens per solved task", "Budget-exhaustion rate"],
    stretch: ["Parallel tool calls", "Compare against OpenAI Agents SDK on the same eval"],
  }),
  defineProject({
    id: "lab-pii-redactor", name: "PII & Secrets Redaction Middleware", tier: 1, categories: ["Security", "AI"],
    hours: [14, 20], unlockWeek: 24,
    roadmap: { weeks: [15, 24], mastery: ["m-rag", "m-net"], resources: ["owasp", "owasp-cheat", "owasp-top10"], dsa: ["Tries", "Sliding Window"], gates: ["P3"], certs: ["AWS Security – Specialty"] },
    prerequisites: [],
    objective: "Stop sensitive data from leaving your network inside prompts: detect, redact and (reversibly) tokenize PII and secrets before any LLM call.",
    problem: "Customer emails, logs and tickets are full of PII and credentials. Sending them to third-party models is a compliance incident waiting to happen.",
    users: ["Security/platform teams approving LLM usage", "Any Lab project handling user data"],
    skills: ["Regex + checksum validators (Luhn, IBAN)", "Entropy-based secret detection", "NER models", "Precision/recall trade-offs"],
    tech: ["Python", "Presidio or spaCy NER", "FastAPI middleware", "pytest + hypothesis"],
    arch: {
      api: "Middleware/SDK: redact(request) → call → restore(response)",
      app: "Detector pipeline (patterns, validators, NER, entropy) with per-entity policies (mask, hash, tokenize, block)",
      data: "Short-lived token vault (Redis with TTL) for reversible placeholders",
    },
    features: [
      "Detectors for emails, phones (incl. Indian formats), cards, Aadhaar/PAN-shaped IDs, API keys, JWTs",
      "Reversible tokenization so answers can be re-hydrated for the user",
      "Policy file per entity type and per destination model",
      "Evaluation on a labelled set of 300 synthetic snippets",
    ],
    milestones: ["Pattern + validator detectors", "NER + entropy detectors", "Reversible vault", "Precision/recall report"],
    testing: ["Property-based tests: redaction is idempotent and restore(redact(x)) == x", "Labelled evaluation in CI"],
    security: ["Vault encrypted at rest with TTL", "No raw values in logs"],
    deployment: ["pip package + FastAPI example"],
    benchmarks: ["Precision and recall per entity type", "Added latency per 1k tokens"],
    stretch: ["Language support beyond English", "Browser extension that redacts before paste"],
  }),
  defineProject({
    id: "lab-aws-deploy-baseline", name: "Production Deploy Baseline on AWS", tier: 1, categories: ["Cloud", "Backend"],
    hours: [18, 28], unlockWeek: 25,
    roadmap: { weeks: [8, 10, 11, 12, 14, 15, 19, 25], mastery: ["m-net", "m-devops"], resources: ["aws-docs", "terraform", "docker", "gha"], dsa: [], gates: ["P2", "P3"], certs: ["AWS Certified Solutions Architect – Associate", "HashiCorp Certified: Terraform Associate (004)"] },
    prerequisites: [],
    objective: "Deploy a containerized API + Postgres to AWS entirely with Terraform — networking, IAM, secrets, TLS, alarms and a budget — then tear it down with one command.",
    problem: "Certifications teach services; employers want someone who can ship a secure, observable, cost-bounded deployment and explain every resource. This is the SAA proof-of-work.",
    users: ["You (SAA + Terraform proof-of-work)", "Small teams moving off a PaaS"],
    skills: ["VPC, subnets, routing, security groups", "IAM least privilege + task roles", "ECS Fargate / App Runner", "Terraform modules and remote state", "Cost awareness"],
    tech: ["Terraform", "AWS (VPC, ECS Fargate, ALB, RDS, Secrets Manager, ACM, CloudWatch, Budgets)", "Docker", "GitHub Actions (OIDC)"],
    arch: {
      api: "FastAPI container behind an ALB with HTTPS (ACM)",
      data: "RDS Postgres in private subnets; automated backups",
      infra: "Terraform modules: network, database, service, observability; remote state in S3 with locking; GitHub OIDC deploy role",
      observability: "CloudWatch logs, alarms on 5xx and latency, AWS Budgets alert",
    },
    features: [
      "Public/private subnet design with no public database",
      "Secrets injected from Secrets Manager — none in images or Git",
      "CI deploy via GitHub OIDC (no long-lived AWS keys)",
      "Architecture diagram and a monthly cost estimate",
      "`make destroy` leaves nothing billable",
    ],
    milestones: ["Network + DB modules", "Service + ALB + TLS", "OIDC CI deploy", "Alarms, budget, diagram, teardown"],
    testing: ["terraform validate + tflint + checkov in CI", "Smoke test hits /health after deploy"],
    observability: ["Alarms routed to email/Slack"],
    security: ["IAM policies scoped to exact ARNs", "Security groups reference each other, not CIDRs"],
    deployment: ["Terraform apply from CI on main; plan on PR"],
    benchmarks: ["Monthly cost estimate vs actual", "Cold deploy time", "RTO for a DB restore drill"],
    stretch: ["Blue/green with CodeDeploy", "Same app on EKS for comparison (feeds Phase 6)"],
  }),
  defineProject({
    id: "lab-repo-map", name: "Repository Map CLI", tier: 1, categories: ["DevTools", "RAG"],
    hours: [14, 22], unlockWeek: 21,
    roadmap: { weeks: [19, 20, 21], mastery: ["m-rag", "m-dsa2"], resources: ["pgvector", "anthropic-docs"], dsa: ["Trees", "Graphs", "Tries"], gates: ["P3"] },
    prerequisites: [],
    objective: "Index a codebase into a symbol graph plus embeddings so a developer can ask “where is X handled?” and get file:line answers with evidence.",
    problem: "Joining a large codebase costs weeks. Grep finds strings, not concepts; chat tools hallucinate paths. A citation-first code map is the base layer of every coding assistant.",
    users: ["Engineers onboarding to large repos", "Your later PR-review and codebase-intelligence projects"],
    skills: ["tree-sitter parsing", "Symbol and call graphs", "Code-aware chunking", "Hybrid lexical + semantic search"],
    tech: ["Python or TypeScript", "tree-sitter", "SQLite + sqlite-vec or pgvector", "Typer/commander CLI"],
    arch: {
      app: "Indexer: parse → symbols (functions, classes, imports) → edges (calls, imports) → chunks per symbol",
      data: "SQLite for symbols/edges, vector table for chunk embeddings, content hash for incremental re-index",
      ai: "Embeddings for chunks; LLM answer step constrained to cite retrieved file:line spans",
    },
    features: [
      "Supports two languages (e.g. TypeScript + Python)",
      "Incremental re-index using file content hashes",
      "`repomap ask` answers with citations; refuses when evidence is missing",
      "`repomap graph <symbol>` prints callers/callees",
    ],
    milestones: ["Parser + symbol table", "Call/import graph", "Embeddings + search", "Cited answers + eval on 25 questions"],
    testing: ["Fixture repos with known symbols and edges", "Citation validity check: every cited line exists"],
    deployment: ["Installable CLI (npm or pipx)"],
    benchmarks: ["Answer accuracy on 25 questions about a real OSS repo", "Index time per 10k LOC", "Incremental re-index time"],
    stretch: ["LSP-based precise references", "VS Code extension surface"],
  }),
];
